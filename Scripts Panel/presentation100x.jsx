if (app.activeWindow.screenMode == ScreenModeOptions.PREVIEW_OFF) {
    app.activeWindow.screenMode = ScreenModeOptions.PRESENTATION_PREVIEW
    app.activeWindow.zoomPercentage = 100
}
else (app.activeWindow.screenMode = ScreenModeOptions.PREVIEW_OFF)
